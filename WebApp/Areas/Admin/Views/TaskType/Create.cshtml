@model WebApp.Areas.Admin.ViewModels.TaskTypeCreateEditeViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>@Shared.Resources.SharedRes.Create</h1>

<h4>@Core.Resources.Domain.DomainEntities.TaskType</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <partial name="_CreateEdite" model="Model"/>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@Shared.Resources.SharedRes.BackToList</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
